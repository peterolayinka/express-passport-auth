{
  "name": "db",
  "version": "1.0.0",
  "description": "my little server db server",
  "main": "index.js",
  "author": "Peter Olayinka",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "json:server": "json-server --watch ./db.json --port 5000"
  },
  "dependencies": {
    "json-server": "^0.14.0"
  }
}
